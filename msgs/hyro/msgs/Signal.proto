/* Message proto file */
syntax = "proto3";

// The namespace where the messages classes will be available
package hyro.msgs;

message Signal
{
  int64   timestamp = 1;
  string   frame_id = 2;
  float      value = 3;
}
